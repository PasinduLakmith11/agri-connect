const sqlite = require('better-sqlite3'); const db = new sqlite('d:/agri/agri-connect/backend/database.sqlite'); console.log('USERS:'); console.table(db.prepare('SELECT id, email, role FROM users').all()); console.log('PRODUCTS:'); console.table(db.prepare('SELECT id, name, farmer_id FROM products').all()); console.log('ORDERS:'); console.table(db.prepare('SELECT id, product_id, buyer_id, status FROM orders').all());